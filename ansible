python --version
sudo yum install https://dl.fedoraproject.org/pub/epel/epel-release-latest-8.noarch.rpm --> for rhel 8
sudo subscription-manager repos --enable ansible-2.9-for-rhel-8-x86_64-rpms
sudo yum install ansible -y

docker ansible combo
----------------------
yum install python3-pip
pip3 install docker
